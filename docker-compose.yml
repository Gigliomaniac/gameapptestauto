version: "3.8"
services:
  gameapptestauto:
    image: gigliomaniac1/domsnaketest:latest
    build: .
    container_name: snakegameautotest10
    ports:
      - "80:3000"
    environment:
      - PORT=3000
    volumes:
      - ./:/app
    command: ["node", "app.js"]